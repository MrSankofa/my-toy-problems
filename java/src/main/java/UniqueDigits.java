import java.util.List;

/*
*
*  Given a range of integers, determine home many numbers have no repeating digits
*
*  each tuple will give you the range.
*  you need to return an array of the solutions from each tuple. Each element in the returned array
*  should be the number of numbers that have unique digits respectively.
*
* */

public class UniqueDigits {

  public static List<Integer> findUniqueNumbers(List<List<Integer>> arr) {

    return List.of(27, 4);
  }
}
